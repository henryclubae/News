// ============================================================================
// PROVIDERS INDEX - CENTRALIZED EXPORTS
// ============================================================================

export { default as ThemeProvider } from './ThemeProvider';
export { useCustomTheme, useThemeToggle, useThemeDetection, themeVariables } from './ThemeProvider';
export type { Theme } from './ThemeProvider';

// Re-export for convenience
export { ThemeProvider as NewsWebsiteThemeProvider } from './ThemeProvider';